version: '3.4'

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      args:
        dev: ""
        simulate_removal: "-s"
    image: libero/search_dev:${IMAGE_TAG:-master}
    volumes:
      - ..:/srv/app
    command: "flask run --host=0.0.0.0"
    ports:
      - "5000:5000"
    env_file:
      - dev.env
